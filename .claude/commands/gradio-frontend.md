请为一个后端应用编写完整的Gradio前端代码(放在frontend目录下），要求如下：

1. **功能完整性**：
   - 实现与后端应用的完整交互流程，包括数据输入、请求提交和结果展示
   - 确保所有后端提供的API端点都有对应的前端交互入口
   - 支持后端所需的各种数据输入格式（如文本、文件、数值等）

2. **用户体验**：
   - 设计直观易用的界面布局，逻辑清晰，操作流程顺畅
   - 添加适当的提示文本和占位符，引导用户正确使用
   - 实现加载状态显示，让用户了解操作进度
   - 添加友好的错误提示和成功反馈

3. **界面设计**：
   - 使用Gradio的Blocks布局系统构建结构化界面
   - 合理分组相关功能，使用分隔符和标题提升可读性
   - 应用适当的主题样式，确保视觉一致性
   - 优化响应式设计，适配不同屏幕尺寸

4. **技术实现**：
   - 使用最新稳定版Gradio API
   - 代码需自包含，包含所有必要的导入和组件定义
   - 实现必要的客户端验证逻辑
   - 提供清晰的代码注释，解释关键功能和实现细节
   - 提供合适的代码规范和自动检查脚本

5. **扩展性考虑**：
   - 代码结构应便于未来功能扩展
   - 对后端可能的变化预留适配空间
   - 如需对后端功能做假设，请在代码注释中明确说明

请确保代码可直接运行，仅依赖标准的`gradio`库及后端所需的必要依赖，并在注释中注明任何额外的依赖项。"