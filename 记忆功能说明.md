# GeoRAG 记忆功能说明

## 功能概述

GeoRAG 现在支持对话记忆功能，可以让AI记住之前的对话内容，实现连续的、有上下文的对话体验。

## 主要特性

1. **会话管理**: 每个对话会话都有唯一的ID，可以跨多次请求保持对话状态
2. **自动记忆**: AI会自动记住历史对话内容，无需重复提供上下文
3. **记忆限制**: 为防止token过多，每个会话最多保留20轮对话
4. **会话清理**: 自动清理过期会话，最多保持100个活跃会话
5. **灵活控制**: 可以选择是否使用记忆功能，支持一次性对话

## API 接口

### 1. 聊天接口 (支持记忆)

**POST** `/chat`

```json
{
  "prompt": "你是一个地理信息专家助手",
  "query": "什么是数字地形模型？",
  "chat_model_name": "qwen-turbo",
  "use_memory": true,
  "session_id": "可选，不提供则创建新会话"
}
```

**响应:**
```json
{
  "response": "AI的回答内容",
  "session_id": "会话ID",
  "message_count": 2
}
```

### 2. 获取会话历史

**POST** `/chat/history`

```json
{
  "session_id": "会话ID"
}
```

**响应:**
```json
{
  "session_id": "会话ID",
  "history": [
    {"role": "system", "content": "系统提示词"},
    {"role": "user", "content": "用户问题"},
    {"role": "assistant", "content": "AI回答"}
  ],
  "message_count": 3
}
```

### 3. 获取所有会话

**GET** `/chat/sessions`

**响应:**
```json
{
  "sessions": {
    "session_id_1": {
      "created_at": "2024-01-01T10:00:00",
      "last_active": "2024-01-01T10:30:00",
      "message_count": 5
    }
  }
}
```

### 4. 删除指定会话

**DELETE** `/chat/sessions/{session_id}`

**响应:**
```json
{
  "message": "会话已删除"
}
```

### 5. 清空所有会话

**POST** `/chat/sessions/clear`

**响应:**
```json
{
  "message": "所有会话已清空"
}
```

## 使用方法

### 1. 启动服务
```bash
python GeoRAGApp.py
```

### 2. 基本使用示例

```python
import requests

# 第一轮对话
response1 = requests.post("http://localhost:7512/chat", json={
    "prompt": "你是一个地理信息专家助手",
    "query": "什么是数字地形模型？",
    "use_memory": True
})

session_id = response1.json()["session_id"]
print(f"AI: {response1.json()['response']}")

# 第二轮对话 - 会记住之前的内容
response2 = requests.post("http://localhost:7512/chat", json={
    "prompt": "你是一个地理信息专家助手",
    "query": "那么DEM和DTM有什么区别呢？",
    "use_memory": True,
    "session_id": session_id
})

print(f"AI: {response2.json()['response']}")
```

### 3. 使用提供的客户端

```python
from chat_memory_example import GeoRAGChatClient

client = GeoRAGChatClient()

# 开始对话
response = client.chat(
    prompt="你是一个地理信息专家助手",
    query="什么是数字地形模型？",
    use_memory=True
)

print(f"AI: {response['response']}")

# 继续对话
response = client.chat(
    prompt="你是一个地理信息专家助手",
    query="那么DEM和DTM有什么区别呢？",
    use_memory=True
)

print(f"AI: {response['response']}")

# 查看历史
history = client.get_chat_history()
print(f"历史记录: {history}")
```

## 运行示例

### 1. 演示模式
```bash
python chat_memory_example.py
```

### 2. 交互式聊天
```bash
python chat_memory_example.py interactive
```

在交互模式中，你可以：
- 输入问题进行对话
- 输入 `history` 查看对话历史
- 输入 `clear` 清空当前会话
- 输入 `sessions` 查看所有会话
- 输入 `quit` 退出

## 配置参数

### 会话管理配置

在 `GeoRAGService` 类中，你可以调整以下参数：

```python
self.max_sessions = 100  # 最大会话数
self.max_memory_length = 20  # 每个会话最大记忆轮次
```

### 记忆控制

```python
# 使用记忆功能
{
    "use_memory": True,
    "session_id": "可选"
}

# 不使用记忆功能（一次性对话）
{
    "use_memory": False
}
```

## 注意事项

1. **Token限制**: 每个会话的记忆有token限制（4000 tokens），超过会自动清理最早的消息
2. **会话清理**: 当会话数超过100个时，会自动清理最老的会话
3. **内存存储**: 会话数据存储在内存中，重启服务后会丢失
4. **并发安全**: 当前实现适合单实例部署，多实例需要考虑会话同步

## 典型应用场景

1. **连续问答**: 用户可以就某个主题进行深入的多轮对话
2. **上下文引用**: AI可以引用之前对话中的内容，提供更准确的回答
3. **知识建构**: 通过多轮对话逐步构建复杂的知识体系
4. **个性化助手**: 记住用户的偏好和之前的讨论内容

## 故障排除

### 常见问题

1. **会话找不到**: 检查session_id是否正确，或者会话是否已被清理
2. **记忆不生效**: 确认 `use_memory` 参数设置为 `true`
3. **响应慢**: 可能是历史消息过多，可以清理会话或调整记忆长度

### 日志检查

服务运行时会输出相关日志，可以帮助诊断问题：

```bash
# 查看服务日志
tail -f georag.log
```

## 扩展建议

1. **持久化存储**: 考虑将会话数据存储到数据库中
2. **会话分组**: 按用户或主题对会话进行分组管理
3. **记忆优化**: 实现更智能的记忆管理，如关键信息提取
4. **多模态记忆**: 支持图片、文件等多种格式的记忆内容 